spring:
  profiles:
    active: local

app:
  logging:
    # For local PII-flow validation only, run with:
    # SPRING_PROFILES_ACTIVE=local APP_LOG_SENSITIVE_DATA=true
    log-sensitive-data: ${APP_LOG_SENSITIVE_DATA:false}

logging:
  level:
    com.example.sqlmcpchatopenrouter.chat: DEBUG
    com.example.sqlmcpchatopenrouter.security: DEBUG
    com.example.sqlmcpchatopenrouter.mcp: DEBUG
